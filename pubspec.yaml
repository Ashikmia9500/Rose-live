name: rose_live
description: Rose-live voice only app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  zego_uikit_prebuilt_live_audio_room: ^3.9.0

flutter:
  uses-material-design: true
